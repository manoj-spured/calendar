package com.calendar.core.populator;

import com.calendar.core.data.AttendeeEventData;
import com.calendar.core.model.AttendeeIndex;

public class AttendeeEventDataPopulator {

    public void populate(AttendeeIndex attendeeIndex, AttendeeEventData attendeeEventData)
    {
       /* attendeeEventData.setId(attendeeIndex.getId());
        attendeeEventData.setAcceptanceStatus(attendeeIndex.getAcceptanceStatus());
        attendeeEventData.setStartTime(attendeeIndex.getStartTime());
        attendeeEventData.setEndTime(attendeeIndex.getEndTime());
        attendeeEventData.setEntityId(attendeeIndex.getEntityId());
        attendeeEventData.setEventNote(attendeeIndex.getEventNote());
        attendeeEventData.setEventType(attendeeIndex.getEventType());
        attendeeEventData.setResponseStatus(attendeeIndex.getResponseStatus());
        attendeeEventData.setScore(attendeeIndex.getScore());
        attendeeEventData.setActive(attendeeIndex.isActive());
        attendeeEventData.setAuthorId(attendeeIndex.getAuthorId());
        attendeeEventData.setCreatedTs(attendeeIndex.getCreatedTs());
        attendeeEventData.setUpdatedTs(attendeeIndex.getUpdatedTs());*/
    }
}
